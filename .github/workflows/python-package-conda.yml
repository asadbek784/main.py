name: Telegram Bot

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  run-bot:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'
    
    - name: Install dependencies
      run: |
        pip install -r yuklash.txt
    
    - name: Run bot
      API: 8501904480:AAGMm74KLah6xb29H8lfzfCNxvsH97EfkdU
      run: |
        python main.py
